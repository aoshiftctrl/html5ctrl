# installs
# install: true

#language
language: php

#php versions
php:
  - '7.1.7'
  - '7.2.1'

# °_°
env: # important!
# °_°
addons:
  apt:
    packages:
    - apache2
    - libapache2-mod-fastcgi

before_script:
  - lsb_release -a
  - lscpu

install:
    - composer install

scripts:
  - ".travis-build/phplint ."
#  - phpunit

# notifications
notifications:
  email: false
  slack:
      on_success: always
      on_failure: always
